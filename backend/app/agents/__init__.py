# agents/__init__.py
# This file initializes the agents package.
# It is empty but required for Python to recognize the 'agents' directory as a package module.
# Downstream modules can import agents directly, e.g., 'from app.agents.search_agent import SearchAgent'.
